camel: 1.0.0
name: at/ad_trust
title: Ad Trust
description: Ad Trust

inputs:
  -
    name: input
    title: input
    parameters:
      - name: text
        type: string
        description: text
        required: true
    routing:
      all:
        action: at/ad_trust
        runtime: cortex/functions
        output: output
outputs:
  -
    name: output
    title: output
    parameters:
      - name: predictions
        type: string
        description: predictions